import { ConfigHeader } from '~/components/config/ConfigHeader';

export default function Products() {
  return (
    <div>
      <ConfigHeader
        title={['Products']}
      />
    </div>
  );
}
